# Secrets never enter this repo (Cruise keys, CLAWHUB_PUBLISH_TOKEN, etc.).
# Keep them in the environment, a secret manager, or a local .env — never in git.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
openclaw.json
openclaw.json5
!examples/openclaw.json5

*.log
.DS_Store
node_modules/
dist/
coverage/
*.tsbuildinfo
*.tgz
reports/
